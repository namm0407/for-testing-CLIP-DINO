# For testing CLIP and grounding DINO

## clip.py

For detecting the objects,
uses the transformers library from Hugging Face to load and run a CLIP model

## DINO.py

For showing (boxing) the detected objects
